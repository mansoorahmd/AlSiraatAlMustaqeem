{
  "name": "@alsiraat/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the Quran corpus (read-only) and the reader's research (read + limited writes)",
  "scripts": {
    "start": "cross-env NODE_NO_WARNINGS=1 tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "smoke": "cross-env NODE_NO_WARNINGS=1 tsx scripts/smoke.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.24.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2"
  }
}
