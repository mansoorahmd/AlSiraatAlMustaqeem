# Quran Foundation Content API credentials — get them at:
# https://api-docs.quran.foundation/request-access/
QF_CLIENT_ID=your_client_id
QF_CLIENT_SECRET=your_client_secret
QF_ENV=production            # or "prelive" for testing

# Optional: default translation IDs to download (see: python download_translations.py --list)
# TRANSLATION_IDS=20,85,131,22,57
