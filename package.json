{
  "name": "alsiraat-almustaqeem",
  "private": true,
  "version": "0.2.0",
  "description": "MQ Research Gate \u2014 a detective-style Quran study app (React SPA + TypeScript API).",
  "main": "electron/main.mjs",
  "workspaces": [
    "app",
    "server",
    "mcp"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev -w server\" \"npm run dev -w app\"",
    "test": "npm test -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w app && npm run typecheck -w @alsiraat/mcp",
    "build": "npm run build -w app",
    "start": "npm run build -w app && npm run start:static -w server",
    "save": "node --no-warnings scripts/save-research.mjs",
    "mcp": "npm start --silent -w @alsiraat/mcp",
    "electron:build": "npm run build -w app && node electron/bundle-server.mjs",
    "electron:rebuild": "node electron/rebuild-native.mjs",
    "electron:rebuild:compile": "electron-rebuild -f -o better-sqlite3",
    "electron:dev": "npm run electron:build && npm run electron:rebuild && electron .",
    "electron:run": "electron .",
    "desktop:dist": "npm run electron:build && electron-builder"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.0",
    "concurrently": "^9.1.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.1.0",
    "esbuild": "^0.24.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.5.0"
  }
}
